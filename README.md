# **Telegram Bot (Aiogram 3.x)**

Этот проект — простой Telegram-бот, написанный на **Python** с использованием библиотеки **aiogram 3.x**.
Бот обрабатывает команду `/start`, приветствует пользователя по имени и работает как echo-бот — повторяет любые отправленные ему сообщения.

---

## 🚀 **Функциональность**

* Ответ на команду **/start**
* Отправка приветствия пользователю
* Эхо-ответ на любые входящие текстовые сообщения
* Разделение логики по роутерам
* Загрузка конфигурации из `.env`

---

## 🔧 **Технологии**

* **Python 3.10+**
* **aiogram 3.x**
* **dotenv** для конфигурации
* **asyncio** для асинхронной работы

---

## ⚙️ **Установка и запуск**

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/kIwwwww1/Aiogram-course.git
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Создайте файл `.env`

В корне проекта:

```
TOKEN=ваш_токен_бота
```

### 4. Запустите бота из корня проекта

```bash
python3 -m lesson_1.main
```

---

## 📌 **Как это работает**

* В `main.py` инициализируется бот и диспетчер, подключаются роутеры и запускается polling.
* В `user_router.py` находятся обработчики:

  * `/start` — приветственное сообщение.
  * Любой текст — бот отвечает тем же текстом.
